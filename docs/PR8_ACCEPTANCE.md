# PR8 acceptance

PR8 may move from draft only when:

- all exposed BDD classifier tools create valid Rust semantic elements
- all exposed owned-feature tools enforce Rust owner/type/multiplicity constraints
- classifier presentations persist and reopen without semantic downgrade
- repository drag reuses existing semantic identity
- relationship creation works across valid migrated classifiers
- BDD compartments reflect owned semantic features
- no JavaScript-only semantic state is introduced
- CI is green
- local functional testing passes
